interface:
  display_name: "Suwappu Yield Farmer"
  short_description: "Repo-specific patterns and workflows for suwappu-yield-farmer"
  default_prompt: "Use the suwappu-yield-farmer repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true