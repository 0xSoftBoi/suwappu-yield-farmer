{
  "name": "suwappu-yield-farmer",
  "version": "2.0.0",
  "description": "Operations-ready, read-only Suwappu/Morpho lending market monitor",
  "type": "module",
  "bin": {
    "suwappu-yield": "src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/cli.ts markets",
    "check": "tsc --noEmit",
    "test": "bun test",
    "build": "mkdir -p dist && bun build src/cli.ts --compile --outfile dist/suwappu-yield",
    "verify": "bun run check && bun test && bun run build && npm audit --audit-level=high"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  },
  "engines": {
    "bun": ">=1.3.14"
  },
  "packageManager": "npm@11.9.0",
  "license": "MIT"
}
